# Authentication for the real-Zephyr e2e flow (`pnpm e2e:zephyr`).
#
# Copy this file to `.env.e2e` and replace the placeholder with a personal
# access token from the Zephyr dashboard. Production API endpoints are used by
# default and do not need to be configured here.

ZE_SECRET_TOKEN=sntryu_replace_me
